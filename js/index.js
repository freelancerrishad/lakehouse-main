function modalLoad(){
    const authenticationModal = document.getElementById('authentication-modal');
    authenticationModal.setAttribute('aria-hidden','false');
    authenticationModal.classList.remove("hidden")
}